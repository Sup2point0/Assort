{
  "name": "Assort",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {},
  "updateContentCommand": "git submodule update --init && cd .site && npm install && cd ../.stranger-quarkdown && bundle install && cd ..",

  "customizations": {
    "vscode": {
      "extensions": []
    },
    "settings": {
      "files.eol": "\n",
      "editor.tabSize": 2
    }
  }
}
